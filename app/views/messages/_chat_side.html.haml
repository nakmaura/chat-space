.side_bar
  .side-header
    .side-header__box
      %p.side-header__box__name
        = current_user.name
      %ul.side-header__box__icons
        %li.side-header__box__icons__edit
          =link_to new_group_path(@group) do
            =icon('fa','edit')
        %li.side-header__box__icons__conf
          =link_to edit_user_path(current_user)do
            =icon('fa','cog')
  .side-groups
    - current_user.groups.each do |group|
      .group
        =link_to"#"do
          %p.group__name
            = group.name
          %p.group__lastlog
            メッセージはまだありません。
    
      
